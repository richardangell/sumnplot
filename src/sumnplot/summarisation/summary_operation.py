"""Enum for the different types of summary operations."""

from enum import Enum


class SummaryOperation(Enum):
    """Enum for the different types of summary operations."""

    SUM = "sum"
    WEIGHTED_AVERAGE = "weighted_average"
