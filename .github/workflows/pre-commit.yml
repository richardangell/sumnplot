name: prek

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  prek:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v7.0.1
    - uses: j178/prek-action@v2.0.6
  